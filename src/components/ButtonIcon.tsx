import { IconButton } from "@lifesg/react-design-system";
import styled from "styled-components";

export const ButtonIcon = styled(IconButton)`
    color: #000;
    border-radius: 0.5rem;
    padding: 0.5rem;
	transition: all 0.15s ease-out;
`;
